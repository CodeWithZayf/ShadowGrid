# ── Server ──────────────────────────────────────────
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# ── Camera IPs ──────────────────────────────────────
# Configure cameras in cameras.json (see cameras.json.example)

# ── Detection thresholds (optional overrides) ───────
# CRIME_CONFIDENCE=0.70
# MIL_ANOMALY_THRESHOLD=0.0123

# ── Re-ID ───────────────────────────────────────────
# SUSPECT_MATCH_THRESHOLD=0.70
